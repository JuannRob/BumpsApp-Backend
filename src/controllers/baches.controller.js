const axios = require('axios').default;
const config = require('config');

async function getBaches(req, res){
    try {
        
    } catch (error) {
        
    }
};

module.exports = { getBaches };